{
    "type": "object",
    "title": "SSHAuth",
    "id": "#/definitions/ssh-auth",
    "description": "Information required to login to a ssh server",
    "properties": {
        "host": {
            "description": "IP of the ssh server one need to connect",
            "type": "string"
        },
        "port": {
            "description": "Port the SSH server is lisetining on",
            "type": "integer"
        },
        "username": {
            "description": "Username of the user one want to login as",
            "type": "string"
        },
        "userPassword": {
            "description": "Password of the user one want to login as",
            "type": "string"
        },
        "privateKeyPath": {
            "description": "Private key of the user logging in",
            "type": "string"
        },
        "passphrase": {
            "description": "Passphrase for the private key if encrypted",
            "type": "string"
        }    
    },
    "required": [
        "host",
        "port",
        "username",
        {"oneOf": ["privateKeyPath","userPassword"]}
    ]
}